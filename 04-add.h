// This is a header file. It holds forward declarations.

// This is a header guard. It prevents this header from being included multiple times
// PreProc directive: IF ADD_H is *not* already defined then...
#ifndef ADD_H
// Define it now. Subsequent include attempts will fail the above if statement and skip to the #endif at the bottom of the header.
#define ADD_H

int add(int x, int y);

#endif
